using UnityEngine;

public class ClimbableWall : MonoBehaviour
{
    ///<summary>
    /// Attach this to any wall that can be climbable.
    /// This script is used to help the player controller 
    /// detect where can they climb and where they can't.
    /// </summary>
}
